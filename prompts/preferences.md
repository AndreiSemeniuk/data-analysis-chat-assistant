A user of a data-analysis assistant said something that may express a durable
formatting/communication preference (how they want future reports to look).

Message: {message}

If it contains a durable preference, respond with JSON mapping preference keys to
short values, e.g. {{"format": "tables", "detail_level": "brief"}}.
Use keys from: format, detail_level, language, other.
If it contains no durable preference (just a one-off request), respond with {{}}.
JSON only.
